# Streamlit Secrets Configuration
# This file is for LOCAL development only
# On Streamlit Cloud, use the Secrets management UI

# Database configuration
db_path = "mental_health.db"
# For cloud deployment, consider using a cloud database instead
# Example PostgreSQL: database_url = "postgresql://user:password@host:port/dbname"
